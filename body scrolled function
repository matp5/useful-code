	function menuScroll() {
		if($(this).scrollTop() > 100){
			$('body').addClass('scrolled');
		}else{
			$('body').removeClass('scrolled');
		}
	}
	menuScroll();
	$(window).scroll(function () {
		menuScroll();
	});
